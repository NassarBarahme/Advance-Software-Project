# شرح واجهة المستخدم (GUI) - HealthPal

## 📋 نظرة عامة

هذا المستند يشرح ما تم إنجازه في واجهة المستخدم وما يجب إكماله.

---

## ✅ ما تم إنجازه (من قبل ناصر)

### 1. صفحة تسجيل الدخول والتسجيل (`login.html`)

**الملفات:**
- `public/login.html` - صفحة HTML
- `public/login.css` - ملف التنسيقات
- `public/login.js` - ملف JavaScript

**الميزات:**
- ✅ تصميم حديث مع تأثيرات زجاجية (Glass Morphism)
- ✅ صفحة واحدة تحتوي على:
  - تبويب تسجيل الدخول (Login)
  - تبويب التسجيل (Sign Up)
- ✅ إدخالات النماذج:
  - البريد الإلكتروني
  - كلمة المرور (مع إمكانية إظهار/إخفاء)
  - الاسم الكامل (للتسجيل)
  - رقم الهاتف (للتسجيل)
  - نوع الحساب (Role): Patient, Doctor, Donor, NGO, Pharmacy
  - التخصص (يظهر فقط للطبيب)
  - تأكيد كلمة المرور
- ✅ التكامل مع API:
  - `/api/auth/login` - تسجيل الدخول
  - `/api/auth/register` - التسجيل
- ✅ حفظ البيانات:
  - حفظ `accessToken` في `localStorage`
  - حفظ بيانات المستخدم في `localStorage`
- ✅ إعادة التوجيه:
  - بعد تسجيل الدخول: يوجه إلى `/dashboard.html`
  - بعد التسجيل: يوجه إلى `/dashboards/{role}.html` (لكن هذا غير موجود حالياً)

**التصميم:**
- ألوان حديثة مع تدرجات
- تأثيرات انتقالية سلسة
- تصميم متجاوب (Responsive)

---

## 🚀 ما تم إضافته الآن (Dashboard)

### 1. صفحة Dashboard الرئيسية (`dashboard.html`)

**الملفات الجديدة:**
- `public/dashboard.html` - صفحة Dashboard الرئيسية
- `public/dashboard.css` - ملف التنسيقات
- `public/dashboard.js` - ملف JavaScript

**الميزات:**

#### أ) Sidebar Navigation (القائمة الجانبية)
- ✅ شعار HealthPal
- ✅ قوائم مختلفة حسب نوع المستخدم:
  - **Patient (مريض)**: الحالات الطبية، الاستشارات، طلبات الأدوية، الصحة النفسية، مجموعات الدعم
  - **Doctor (طبيب)**: الاستشارات، الحالات الطبية
  - **Donor (متبرع)**: التبرعات
  - **NGO (منظمة)**: المنظمات، التبرعات
  - **Pharmacy (صيدلية)**: طلبات الأدوية، المخزون الطبي
  - **Admin (مدير)**: المستخدمون، جميع الحالات، المنظمات، إعدادات النظام
- ✅ زر تسجيل الخروج

#### ب) Top Bar (الشريط العلوي)
- ✅ زر القائمة (للموبايل)
- ✅ عنوان الصفحة الحالية
- ✅ معلومات المستخدم (الاسم + الدور)

#### ج) صفحات المحتوى
- ✅ **لوحة التحكم**: إحصائيات وإجراءات سريعة
- ✅ **الملف الشخصي**: عرض وتعديل بيانات المستخدم
- ✅ **الحالات الطبية**: جدول يعرض جميع الحالات
- ✅ **الاستشارات**: قائمة بالاستشارات
- ✅ **التبرعات**: جدول بالتبرعات
- ✅ **طلبات الأدوية**: جدول بطلبات الأدوية
- ✅ **الصحة النفسية**: قائمة بالجلسات
- ✅ **مجموعات الدعم**: قائمة بالمجموعات
- ✅ **المنظمات**: شبكة ببطاقات المنظمات
- ✅ **المخزون الطبي**: جدول بالمخزون
- ✅ **المستخدمون** (Admin فقط): جدول بجميع المستخدمين
- ✅ **إعدادات النظام** (Admin فقط): صفحة الإعدادات

#### د) الميزات التقنية
- ✅ فحص المصادقة (Authentication Check)
- ✅ إعادة التوجيه إلى صفحة تسجيل الدخول إذا لم يكن المستخدم مسجلاً
- ✅ استدعاءات API مع Token
- ✅ معالجة الأخطاء
- ✅ رسائل النجاح/الخطأ
- ✅ تصميم متجاوب (Responsive) للموبايل

---

## 📝 ما يجب إكماله

### 1. النماذج (Modals) لإضافة البيانات

حالياً، جميع أزرار "إضافة" تعرض رسالة "قيد التطوير". يجب إنشاء:

#### أ) نموذج إضافة حالة طبية
```javascript
function showAddMedicalCaseModal() {
  // يجب إنشاء modal مع:
  // - نوع الحالة
  // - الوصف
  // - الأولوية
  // - الملفات المرفقة (إن وجدت)
}
```

#### ب) نموذج إضافة استشارة
```javascript
function showAddConsultationModal() {
  // يجب إنشاء modal مع:
  // - اختيار الطبيب
  // - موضوع الاستشارة
  // - الوصف
  // - التاريخ والوقت المفضل
}
```

#### ج) نموذج إضافة تبرع
```javascript
function showAddDonationModal() {
  // يجب إنشاء modal مع:
  // - نوع التبرع (مالي، أدوية، معدات)
  // - المبلغ/الكمية
  // - الوصف
}
```

#### د) نموذج إضافة طلب دواء
```javascript
function showAddMedicationRequestModal() {
  // يجب إنشاء modal مع:
  // - اسم الدواء
  // - الكمية
  // - الوصف
  // - اختيار الصيدلية
}
```

#### هـ) نماذج أخرى:
- إضافة جلسة صحة نفسية
- إضافة مجموعة دعم
- إضافة منظمة (Admin)
- إضافة عنصر للمخزون (Pharmacy)

### 2. صفحات العرض التفصيلية

حالياً، جميع أزرار "عرض" تعرض رسالة فقط. يجب إنشاء:

- صفحة عرض حالة طبية تفصيلية
- صفحة عرض استشارة (مع نظام الرسائل)
- صفحة عرض تبرع
- صفحة عرض طلب دواء
- صفحة عرض مستخدم (Admin)

### 3. وظائف التعديل والحذف

- تعديل الحالات الطبية
- تعديل الاستشارات
- تعديل التبرعات
- تعديل طلبات الأدوية
- حذف المستخدمين (Admin)
- حذف المنظمات (Admin)

### 4. نظام الرسائل في الاستشارات

- عرض الرسائل في الاستشارة
- إرسال رسالة جديدة
- تحديث الرسائل في الوقت الفعلي (Real-time) - يمكن استخدام Socket.io

### 5. نظام البحث والفلترة

- بحث في الحالات الطبية
- فلترة حسب الحالة (Status)
- بحث في المنظمات
- بحث في المستخدمين (Admin)

### 6. نظام الإشعارات

- إشعارات للمستخدمين عند:
  - قبول/رفض طلب
  - رسالة جديدة في استشارة
  - تحديث حالة

### 7. تحسينات إضافية

- **Pagination**: تقسيم البيانات إلى صفحات
- **Sorting**: ترتيب الجداول
- **Export**: تصدير البيانات (PDF, Excel)
- **Charts**: رسوم بيانية للإحصائيات
- **Dark Mode**: وضع الظلام
- **Multi-language**: دعم اللغة الإنجليزية

---

## 🗂️ هيكل الملفات الحالي

```
public/
├── login.html          ✅ (جاهز - من ناصر)
├── login.css          ✅ (جاهز - من ناصر)
├── login.js           ✅ (جاهز - من ناصر)
├── dashboard.html     ✅ (جاهز - جديد)
├── dashboard.css      ✅ (جاهز - جديد)
└── dashboard.js       ✅ (جاهز - جديد)
```

---

## 🔗 API Endpoints المستخدمة

### Authentication
- `POST /api/auth/login` - تسجيل الدخول
- `POST /api/auth/register` - التسجيل

### Users
- `GET /api/users/me` - الملف الشخصي
- `GET /api/users` - جميع المستخدمين (Admin)
- `GET /api/users/:id` - مستخدم محدد
- `PUT /api/users/:id` - تحديث المستخدم

### Medical Cases
- `GET /api/medical-cases` - جميع الحالات
- `GET /api/medical-cases/:id` - حالة محددة
- `POST /api/medical-cases` - إضافة حالة (Patient)

### Consultations
- `GET /api/consultations` - جميع الاستشارات
- `GET /api/consultations/:id` - استشارة محددة
- `POST /api/consultations` - إضافة استشارة
- `GET /api/consultations/:id/messages` - رسائل الاستشارة
- `POST /api/consultations/:id/messages` - إرسال رسالة

### Donations
- `GET /api/donations` - جميع التبرعات
- `GET /api/donations/:id` - تبرع محدد
- `POST /api/donations` - إضافة تبرع

### Medication Requests
- `GET /api/medication_requests` - جميع الطلبات
- `GET /api/medication_requests/:id` - طلب محدد
- `POST /api/medication_requests` - إضافة طلب

### Mental Health
- `GET /api/mental_health_sessions` - جميع الجلسات
- `POST /api/mental_health_sessions` - إضافة جلسة

### Support Groups
- `GET /api/support_groups` - جميع المجموعات
- `POST /api/support_groups` - إضافة مجموعة

### NGOs
- `GET /api/ngos` - جميع المنظمات
- `GET /api/ngos/:id` - منظمة محددة
- `POST /api/ngos` - إضافة منظمة (Admin)
- `PUT /api/ngos/:id` - تحديث منظمة (Admin)
- `DELETE /api/ngos/:id` - حذف منظمة (Admin)

### Medical Inventory
- `GET /api/medical_inventory` - جميع المخزون
- `GET /api/medical_inventory/:id` - عنصر محدد
- `POST /api/medical_inventory` - إضافة عنصر
- `PATCH /api/medical_inventory/:id` - تحديث عنصر

---

## 🎨 التصميم والألوان

### الألوان المستخدمة:
- **الأزرق الأساسي**: `#3498db` - للأزرار والروابط
- **الأزرق الداكن**: `#2980b9` - للتدرجات
- **الرمادي الداكن**: `#2c3e50` - للنصوص الرئيسية
- **الرمادي الفاتح**: `#7f8c8d` - للنصوص الثانوية
- **الأحمر**: `#e74c3c` - للأخطاء والأزرار الخطرة
- **الأخضر**: `#27ae60` - للنجاح
- **الأصفر**: `#f39c12` - للتحذيرات

### الخطوط:
- `'Segoe UI', Tahoma, Geneva, Verdana, sans-serif`

---

## 📱 التصميم المتجاوب

- ✅ Desktop: Sidebar ثابت على اليسار
- ✅ Tablet: Sidebar قابل للطي
- ✅ Mobile: Sidebar مخفي (يظهر عند الضغط على زر القائمة)

---

## 🚀 كيفية المتابعة

### الخطوة 1: اختبار ما تم إنجازه
1. شغل السيرفر: `npm start`
2. افتح المتصفح: `http://localhost:3000/login.html`
3. سجل دخول أو أنشئ حساب جديد
4. جرب التنقل بين الصفحات

### الخطوة 2: إكمال النماذج (Modals)
ابدأ بإنشاء نماذج لإضافة البيانات. يمكنك استخدام نفس نمط التصميم الموجود.

### الخطوة 3: إكمال صفحات العرض
أنشئ صفحات لعرض التفاصيل الكاملة لكل عنصر.

### الخطوة 4: إضافة الميزات المتقدمة
- البحث والفلترة
- Pagination
- Charts
- Real-time updates

---

## 💡 نصائح للتطوير

1. **استخدم Console.log**: لفحص البيانات القادمة من API
2. **افحص Network Tab**: في Developer Tools لرؤية طلبات API
3. **اختبر مع أدوار مختلفة**: تأكد أن كل دور يرى فقط ما يجب أن يراه
4. **اختبر على الموبايل**: تأكد أن التصميم يعمل على جميع الأجهزة

---

## 📞 ملاحظات مهمة

- جميع طلبات API تحتاج إلى `Authorization: Bearer {token}` في الـ Headers
- إذا كان Token غير صالح، يتم إعادة التوجيه تلقائياً إلى صفحة تسجيل الدخول
- البيانات محفوظة في `localStorage` - يمكن مسحها من Developer Tools

---

## ✅ قائمة التحقق (Checklist)

### الأساسيات
- [x] صفحة تسجيل الدخول
- [x] صفحة Dashboard الرئيسية
- [x] Navigation Sidebar
- [x] صفحات العرض الأساسية
- [ ] نماذج الإضافة (Modals)
- [ ] صفحات العرض التفصيلية
- [ ] وظائف التعديل
- [ ] وظائف الحذف

### الميزات المتقدمة
- [ ] البحث والفلترة
- [ ] Pagination
- [ ] Charts/Graphs
- [ ] Real-time Updates
- [ ] الإشعارات
- [ ] Export Data

---

**آخر تحديث**: الآن  
**الحالة**: Dashboard الأساسي جاهز، يحتاج إلى إكمال النماذج والوظائف

